'use strict';

// AboutCtrl
// Controller of the angularApp

angular.module( 'myApp' ).controller( 'AboutCtrl', ['$scope', function( $scope ) {
  $scope.awesomeThings = [
    'HTML5 Boilerplate',
    'AngularJS',
    'Karma'
  ];
} ] );
